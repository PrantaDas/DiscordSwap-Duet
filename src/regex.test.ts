import { describe, expect, test } from "@jest/globals";


describe("Test RegEx", () => {
  // Test If I can create a web3/ethers instance with Wallet Class
  test("Web3 instance creation", () => {
    expect(1).toHaveProperty('ready');
  });
});